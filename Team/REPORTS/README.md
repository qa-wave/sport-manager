# REPORTS — sport-manager

Generované reporty (cleanup, audit, analýza). Aktuální obsah:

- `unused-code.md` — výstup `knip` / `ts-prune`
- `unused-deps.md` — výstup `depcheck`

Tyto soubory jsou **kandidáti k řešení**, neprovedené úpravy. Vždy verifikuj
před smazáním kódu (false positives jsou běžné u dynamicky volaných exportů).
